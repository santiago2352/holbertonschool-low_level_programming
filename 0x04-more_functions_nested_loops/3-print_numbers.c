#include "holberton.h"

/**
 * print_numbers - prints the numbers, from 0 to 9,
 */
void print_numbers(void)
{
int count;

for (count = 0; i < 10; count++)
{
_putchar(count + '0');
}

_putchar('\n');
}
