#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first parameter to be multiplied
 * @b: secon parameter to be multiplied
 * Return: the result of the operation
 */
int mul(int a, int b)
{
int num;

num = a * b;

return (num);
}
